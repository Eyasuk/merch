import { Button } from 'antd';
import styles from './page.module.scss';

export default async function Index() {
  return (
    <div className={styles.page}>
      start overss
      <Button type="primary" title="gngbhg" />
    </div>
  );
}
